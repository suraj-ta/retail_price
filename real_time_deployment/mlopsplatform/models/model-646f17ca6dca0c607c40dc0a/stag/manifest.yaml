apiVersion: machinelearning.seldon.io/v1
kind: SeldonDeployment
metadata:
  name: model-646f17ca6dca0c607c40dc0a
  namespace: stag
spec:
  predictors:
  - graph:
      envSecretRefName: sa-access-secret
      implementation: SKLEARN_CUSTOM_SERVER
      modelUri: azureblob:model-artifacts/model-646f17ca6dca0c607c40dc0a/stag/2
      name: model-646f17ca6dca0c607c40dc0a-graph
      parameters:
      - name: method
        type: STRING
        value: predict
      serviceAccountName: myserviceaccount
      storageInitializerImage: seldonio/rclone-storage-initializer:1.15.0-dev
    name: default
    replicas: 1
